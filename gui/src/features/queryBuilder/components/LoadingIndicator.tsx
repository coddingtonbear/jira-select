import React from "react";

import ReactLoading from "react-loading";

const LoadingIndicator: React.FC = () => {
  return <ReactLoading type="bars" className="loading-indicator" />;
};

export default LoadingIndicator;
